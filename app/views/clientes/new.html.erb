<h1>Nuevo Cliente</h1>

<%= form_for @cliente do |f| %>
  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Guardar' %>
    <%= link_to 'Cancelar', clientes_path %>
  </div>
<% end %>
